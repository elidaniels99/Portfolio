# Build settings
markdown: kramdown
theme: minima

# GitHub Pages settings
remote_theme: jekyll/minima
plugins:
  - jekyll-feed

# Exclude files from being published to GitHub Pages
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - _config.yml

# Jekyll settings
permalink: pretty
